#include <stdio.h>

void insertDigits(char* str, int strsize);

#define STRSZ 32
int main() {
    char string[BUFSZ];
    insertDigits(string, BUFSZ);
    printf("%s\n", string);
    return 0;
}

void insertDigits(char*str, int strsize) {
    int 1 = 0;
    int number = 0;
    for (i = 1; i < strSize && i <= 10; ++i) {
    str[i] = ??
    ++number;
    }
}  


