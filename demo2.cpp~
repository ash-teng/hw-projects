#include <iostream>
#include <string>
using namespace std;

int main() {
    int iValue = 0;
    string work = "";
    string name = "";
    cout << "Enter a number followed by a string: ";
    cin >> iValue;
    cin >> work;
    getline(cin, name);
    cout << "Enter your name:";
//    cin >> name;
    getline(cin, name);
    cout << "The new value of iValue is " << iValue << endl;
    cout << "Hello" << name << endl;
    return 0;
}
